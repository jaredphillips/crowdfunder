<h1>My Projects</h1>
<%= link_to 'New Project', :new_my_project %>	
<% @projects.each do |project| %>
	<div class="project">
		<h3><%= link_to project.title, [:my, project] %></h3>
		<h5><%= project.teaser %></h5>
		<p><%= project.description %></p>
	</div>
<% end %>